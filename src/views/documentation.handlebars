<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Archivos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css" />
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <h1 class="navbar-brand fs-3 p-0">Carga de archivos</h1>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item d-flex align-items-center">
          <a class="nav-link d-flex p-0 fs-5 align-items-center" href="/shop/sessions/profile"><i class="bi bi-person-circle ms-3"></i></a>
        </li>
      </ul>
    </div>
  </nav>

  <div class="container mt-5">
    <div class="row">
      <div class="col-md-8 offset-md-2">
        <!-- Formulario para subir imagen de perfil -->
        <div class="card">
          <div class="card-header">
            <h2>Perfil</h2>
          </div>
          <div class="card-body">
            <form action="/api/users/upload/profile" method="post" enctype="multipart/form-data">
              <div class="mb-3">
                <label for="profile_image" class="form-label">Imagen de Perfil:</label>
                <input type="file" name="profile_image" id="profile_image" class="form-control" accept="image/*" />
              </div>
              <button type="submit" class="btn btn-success">Subir Archivo</button>
            </form>
          </div>
        </div>

        <!-- Formulario para subir imagen de producto -->
        <div class="card mt-4">
          <div class="card-header">
            <h2>Producto</h2>
          </div>
          <div class="card-body">
            <form action="/upload-product-image" method="post" enctype="multipart/form-data">
              <div class="mb-3">
                <label for="product_image" class="form-label">Imagen de Producto:</label>
                <input type="file" name="product_image" id="product_image" class="form-control" accept="image/*" />
              </div>
              <button type="submit" class="btn btn-danger">Subir Archivo</button>
            </form>
          </div>
        </div>

        <!-- Formulario para subir archivos PDF -->
        <div class="card mt-4">
          <div class="card-header">
            <h2>Documentación</h2>
          </div>
          <div class="card-body">
            <form action="/api/users/{{_id}}/documents" method="post" enctype="multipart/form-data">
              <div class="mb-3">
                <label for="documents" class="form-label">Documentos:</label>
                <input type="file" name="documents" id="documents" class="form-control" accept=".pdf" multiple/>
              </div>
              <button type="submit" class="btn btn-warning">Subir Archivos</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script>

  </script>
</body>

</html>
